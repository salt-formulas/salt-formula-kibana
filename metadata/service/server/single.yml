applications:
- kibana
classes:
- service.kibana.support
parameters:
  kibana:
    server:
      enabled: true
      version: latest
      source:
        engine: file
      bind:
        address: 0.0.0.0
        port: 5601
      database:
        engine: elasticsearch
        host: localhost
        port: 9200
